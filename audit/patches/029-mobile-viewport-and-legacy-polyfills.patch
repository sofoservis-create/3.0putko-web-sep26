diff --git a/.migration-backup/frontend/package.json b/.migration-backup/frontend/package.json
index dd968ad..aadd8f1 100644
--- a/.migration-backup/frontend/package.json
+++ b/.migration-backup/frontend/package.json
@@ -40,6 +40,20 @@
     "sharp": "^0.33.5",
     "swiper": "^11.1.14"
   },
+  "//browserslist": [
+    "Next 14 ships a 110 kB (38.5 kB gzip) legacy polyfill bundle to EVERY visitor",
+    "unless a browserslist is declared. Measured on this build, that is 12% of the",
+    "329 kB gzip first-load JS on /login and it is dead weight for every browser",
+    "this site actually supports."
+  ],
+  "browserslist": [
+    "chrome >= 91",
+    "edge >= 91",
+    "firefox >= 90",
+    "safari >= 15",
+    "not dead",
+    "not op_mini all"
+  ],
   "devDependencies": {
     "@types/node": "22.5.1",
     "@types/react": "18.3.4",
diff --git a/artifacts/putko/index.html b/artifacts/putko/index.html
index cbd0c76..ae1b80f 100644
--- a/artifacts/putko/index.html
+++ b/artifacts/putko/index.html
@@ -2,7 +2,12 @@
 <html lang="en">
   <head>
     <meta charset="UTF-8" />
-    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1" />
+    <!-- maximum-scale=1 blocked pinch-zoom entirely, which fails WCAG 2.2
+         SC 1.4.4 (Resize Text) and makes the map, the date picker and the
+         listing address unusable for anyone who needs to magnify them. There is
+         no iOS input-zoom problem left to work around: that is fixed by giving
+         inputs a 16px font size, not by disabling zoom for everyone. -->
+    <meta name="viewport" content="width=device-width, initial-scale=1" />
     <title>Putko</title>
     <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
     <link rel="preconnect" href="https://fonts.googleapis.com">
